#include "PlayerHand.h"

void PlayerHand::RenderCall(SDL_Renderer* renderable)
{
	// work out and set location of each card
	// set locations
	// render all

	// in terms of setting out locations
	// put one on each furthest side, then each side but in more
	// then repeat till you reach the middle
	return;
}
